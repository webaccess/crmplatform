"use strict";

/**
 * crm-plugin.js controller
 *
 * @description: A set of functions called "actions" of the `crm-plugin` plugin.
 */
module.exports = {
  /**
   * Default action.
   *
   * @return {Object}
   */
  find: async (ctx) => {
    // let c = find(ctx);
    return "strapi";
  },
};
