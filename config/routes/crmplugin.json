{
  "routes": [
    {
      "method": "GET",
      "path": "/generateRoutes",
      "handler": "crmplugin.generateRoutes",
      "config": {
        "policies": ["plugins::users-permissions.isAuthenticated"]
      }
    }
  ]
}
